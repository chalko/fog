# Maintenance Log: 2026-07-21

## Summary of Operations
Upgraded system memory on Proxmox host `misty` (Minisforum UM760 Slim) from 16 GB to 32 GB. Verified system stability and adjusted ZFS ARC limit configurations to utilize the expanded hardware capacity.

---

## 1. Hardware Upgrade
*   **Action:** Installed an additional 16 GB DDR5 module.
*   **Result:** Proxmox VE host now recognizes 32 GB physical DDR5 RAM.
*   **Hardware verification (`dmidecode -t memory`):**
    *   2x 16 GB modules detected.
    *   Configured Memory Speed: **5600 MT/s** (running at full rated speed in dual-channel configuration).
    *   Total Usable Memory at Host OS: **29.4 GB** (2.6 GB reserved for Ryzen 5 7640HS integrated GPU/system overhead).

---

## 2. Memory Stability Verification
*   **Tool:** `memtester`
*   **Parameters:** Checked 10 GB of system memory for 1 complete loop.
*   **Status:** **Completed Successfully** (Run exited with clean output `Done.`).
*   **Results:** Verified all address patterns and bit flip sequences. No memory faults or address mapping conflicts were encountered.

---

## 3. Host-Level Configuration Updates
*   **ZFS ARC Cache Limit Adjustment (`zfs_arc_max`):**
    *   *Previous Limit:* 2 GB (designed to prevent host memory starvation on the old 16 GB physical capacity).
    *   *New Limit:* 4 GB.
    *   *Action:* Configured reboot persistence via `/etc/modprobe.d/zfs.conf`, regenerated initramfs, and dynamically applied the limit to the running kernel parameters.
*   **VM Resource Allocation Updates (Terraform):**
    *   Bumped default memory resource limits for VM `k8s-worker-01` from **10 GB** (`10240`) to **20 GB** (`20480`) in [provision/variables.tf](file:///home/nick/src/fog/provision/variables.tf) to allow hosting memory-burstable Ollama workloads. Applied successfully via Terraform.

---

## 4. Cluster Reactivation & Maintenance Cleanup
*   **Vault Unsealing:**
    *   Vault restarted in a sealed state post-boot. Created an automated helper script [bin/unseal-vault.sh](file:///home/nick/src/fog/bin/unseal-vault.sh) to quickly load the `/dev/shm/fog/vault.env` cache configuration and sequentially feed the unseal keys from `pass` (requiring YubiKey physical tap confirmation). Fully unsealed.
*   **Workload Rescheduling:**
    *   Kubernetes nodes `talos-47e-fbr` and `talos-g3w-4lg` were uncordoned via `kubectl` to allow pods (including Ollama, LiteLLM, and system core pods) to schedule onto the newly scaled worker VM resources. Verified all pods resumed to `Running` status.
*   **Codebase Cleanup:**
    *   Removed unused `k8s` environment configuration logic from [bin/load-env.sh](file:///home/nick/src/fog/bin/load-env.sh) and cleaned up unused keys. Committed changes locally.

