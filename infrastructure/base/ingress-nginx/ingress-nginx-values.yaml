# Configuration values for ingress-nginx on bare-metal Talos cluster
controller:
  hostNetwork: true
  dnsPolicy: ClusterFirstWithHostNet
  kind: DaemonSet
  service:
    type: ClusterIP
  publishService:
    enabled: false
